[project]
name = "gpu-preference-manager"
version = "0.1.0"
description = "Windows 11 GPU Preference Manager & Live Monitor"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6.0",
    "psutil>=5.9.0",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0.0",
]

[tool.uv]
package = false
